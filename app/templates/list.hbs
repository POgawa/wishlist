
<h1> {{model.name}}</h1>

{{outlet}}
<ul>
  {{#each book in model.books}}
    <li>{{#link-to 'book' book.title}}{{book.title}}{{/link-to}}   <small>by {{book.author}}</small></li>
  {{/each}}
</ul>

{{#link-to 'list.new-book'}}Add a Book{{/link-to}}
